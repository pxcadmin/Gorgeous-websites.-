export interface Offer {
  name: string;
  best: string;
  price: string;
  includes: string[];
  to: string;
  accent?: string;
  summary?: string;
}
export interface PageMeta {
  title: string;
  description: string;
  h1: string;
}
